# Leave empty when using a same-origin /api rewrite/proxy.
VITE_API_BASE_URL=

# Local Vite dev proxy target for /api requests.
VITE_API_PROXY_TARGET=http://localhost:8000

# Google OAuth 2.0 Web Client ID. Leave empty to hide the Google sign-in button.
VITE_GOOGLE_CLIENT_ID=
