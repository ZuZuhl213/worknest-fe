# Cloudflare Pages Redirects
# Prefer this same-origin API proxy when frontend and backend are on different hosts.
# Replace the placeholder with the deployed backend origin before production release.
/api/* https://worknest-be.onrender.com/api/:splat 200

# SPA fallback for direct refreshes on React Router routes.
/* /index.html 200
